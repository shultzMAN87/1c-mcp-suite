
#Область ПрограммныйИнтерфейс

Функция ДобавитьЗапись(пСтруктураЗаписи) Экспорт	
	сткРез = АукОбщийСервер.ДобавитьЗаписьВРегистрСведений(пСтруктураЗаписи, "АукИтогиЗавершенияЭтапов");
	
	Если Не сткРез.Успех Тогда
		ВызватьИсключение сткРез.Ошибка;	
	КонецЕсли;
КонецФункции

Функция УдалитьЗапись(пСтруктураЗаписи) Экспорт
	
	Возврат АукОбщийСервер.УдалитьНаборЗаписейВРегистреСведений(пСтруктураЗаписи, "АукИтогиЗавершенияЭтапов");
	
КонецФункции	

Функция ИтоговоеЗначениеЛидирующейСтавки(пСткОтбор) Экспорт
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	АукИтогиЗавершенияЭтапов.ПредставлениеРезультатаРасчета КАК ПредставлениеРезультатаРасчета
	|ИЗ
	|	РегистрСведений.АукИтогиЗавершенияЭтапов КАК АукИтогиЗавершенияЭтапов
	|ГДЕ
	|	АукИтогиЗавершенияЭтапов.Аукцион = &Аукцион
	|	И АукИтогиЗавершенияЭтапов.НомерЭтапа = &НомерЭтапа";
	
	Запрос.УстановитьПараметр("Аукцион",	пСткОтбор.Аукцион);
	Запрос.УстановитьПараметр("НомерЭтапа", пСткОтбор.НомерЭтапа);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Если РезультатЗапроса.Пустой() Тогда
		Возврат "";	
	Иначе
		Выборка = РезультатЗапроса.Выбрать();
		
		Выборка.Следующий();
		
		Возврат Выборка.ПредставлениеРезультатаРасчета;	
	КонецЕсли;	
КонецФункции

#КонецОбласти
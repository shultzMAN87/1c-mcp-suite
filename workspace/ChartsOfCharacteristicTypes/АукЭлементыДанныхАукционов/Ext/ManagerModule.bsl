
#Область ПрограммныйИнтерфейс

Функция ЭлементыДанныхАукционовПоТехническимНаименованиям(пМасТехническиеНаименования) Экспорт

	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	АукЭлементыДанныхАукционов.Ссылка КАК ЭлементДанных,
	|	АукЭлементыДанныхАукционов.ТехническоеНаименование КАК ТехническоеНаименование,
	|	АукЭлементыДанныхАукционов.ТипЗначения КАК ТипЗначения
	|ИЗ
	|	ПланВидовХарактеристик.АукЭлементыДанныхАукционов КАК АукЭлементыДанныхАукционов
	|ГДЕ
	|	АукЭлементыДанныхАукционов.ТехническоеНаименование В(&ТехническиеНаименования)";
	
	Запрос.УстановитьПараметр("ТехническиеНаименования", пМасТехническиеНаименования);
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	сооРезультат = Новый Соответствие;
	Пока Выборка.Следующий() Цикл
		сооРезультат.Вставить(Выборка.ТехническоеНаименование, Новый Структура("ЭлементДанных, ТипЗначения", Выборка.ЭлементДанных, Выборка.ТипЗначения));	
	КонецЦикла;	
	
	Возврат сооРезультат;
	
КонецФункции

Функция ЭлементыДанныхАукционовПоНаименованиям(пНаименования) Экспорт 
	
	Возврат СвойстваЭлементовДанныхУниверсально(пНаименования, "Наименование");
	
КонецФункции

Функция СуществуетЭлементСТехническимНаименованием(пТекущийОбъект) Экспорт

	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	АукЭлементыДанныхАукционов.Ссылка КАК Ссылка
	|ИЗ
	|	ПланВидовХарактеристик.АукЭлементыДанныхАукционов КАК АукЭлементыДанныхАукционов
	|ГДЕ
	|	АукЭлементыДанныхАукционов.ТехническоеНаименование = &ТехническоеНаименование
	|	И АукЭлементыДанныхАукционов.Ссылка <> &ТекущийЭлемент";
	
	Запрос.УстановитьПараметр("ТехническоеНаименование",	пТекущийОбъект.ТехническоеНаименование);
	Запрос.УстановитьПараметр("ТекущийЭлемент",				пТекущийОбъект.Ссылка);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Возврат Не РезультатЗапроса.Пустой();
	
КонецФункции

Функция СуществуетЭлементСНаименованием(пТекущийОбъект) Экспорт

	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1
	|	АукЭлементыДанныхАукционов.Ссылка КАК Ссылка
	|ИЗ
	|	ПланВидовХарактеристик.АукЭлементыДанныхАукционов КАК АукЭлементыДанныхАукционов
	|ГДЕ
	|	АукЭлементыДанныхАукционов.Наименование = &Наименование
	|	И АукЭлементыДанныхАукционов.Ссылка <> &ТекущийЭлемент";
	
	Запрос.УстановитьПараметр("Наименование",	пТекущийОбъект.Наименование);
	Запрос.УстановитьПараметр("ТекущийЭлемент",	пТекущийОбъект.Ссылка);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Возврат Не РезультатЗапроса.Пустой();
	
КонецФункции

Функция СписокПредставленийПоМассивуЭлементов(пЭлементыДанных, пОтборПоТипу = Неопределено) Экспорт

	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	АукЭлементыДанныхАукционов.Представление КАК Представление,
	|	АукЭлементыДанныхАукционов.ТипЗначения КАК ТипЗначения
	|ИЗ
	|	ПланВидовХарактеристик.АукЭлементыДанныхАукционов КАК АукЭлементыДанныхАукционов
	|ГДЕ
	|	АукЭлементыДанныхАукционов.Ссылка В(&ЭлементыДанных)";
	
	Запрос.УстановитьПараметр("ЭлементыДанных", пЭлементыДанных);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Если пОтборПоТипу = Неопределено Тогда 
	
		Возврат РезультатЗапроса.Выгрузить().ВыгрузитьКолонку("Представление");
		
	Иначе
		
		Возврат СписокПредставленийПоМассивуЭлементовСОтборомПоТипу(РезультатЗапроса, пОтборПоТипу);	
		
	КонецЕсли;	
	
КонецФункции	

Функция СоответствиеОперандовТехническимНаименованиям(пНаименованияОперандов) Экспорт

	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	""["" + АукЭлементыДанныхАукционов.Наименование + ""]"" КАК Наименование,
	|	АукЭлементыДанныхАукционов.ТехническоеНаименование КАК ТехническоеНаименование
	|ИЗ
	|	ПланВидовХарактеристик.АукЭлементыДанныхАукционов КАК АукЭлементыДанныхАукционов
	|ГДЕ
	|	""["" + АукЭлементыДанныхАукционов.Наименование + ""]"" В(&НаименованияОперандов)";
	
	Запрос.УстановитьПараметр("НаименованияОперандов", пНаименованияОперандов);
	
	сооРезультат = Новый Соответствие;
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл	
		
		сооРезультат.Вставить(Выборка.Наименование, Выборка.ТехническоеНаименование);
		
	КонецЦикла;
	
	Возврат сооРезультат;
	
КонецФункции	

Функция ЗначениеКонстантыПоТехническомуНаименованию(пТехНаименованиеКонстанты) Экспорт
	УстановитьБезопасныйРежим(Истина);
	
	Попытка
		Возврат Вычислить("Константа_" + пТехНаименованиеКонстанты + "()");
	Исключение
		Возврат Неопределено;	
	КонецПопытки;
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции 

Функция СписокПредставленийПоМассивуЭлементовСОтборомПоТипу(пРезультатЗапроса, пОтборПоТипу)
	
	масРезультат = Новый Массив;
	
	Выборка = пРезультатЗапроса.Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		Если АукОбщийКлиентСервер.ТипЗначенияЭлементаПвхСоответствуетТипу(Выборка.ТипЗначения, пОтборПоТипу) Тогда
			
			масРезультат.Добавить(Выборка.Представление);
			
		КонецЕсли;	
		
	КонецЦикла;	
	
	Возврат масРезультат;
	
КонецФункции	

Функция СвойстваЭлементовДанныхУниверсально(пСписок, пРеквизитДляОтбора)

	Запрос = Новый Запрос;
	Запрос.Текст =
	СтрШаблон(
	"ВЫБРАТЬ
	|	АукЭлементыДанныхАукционов.Наименование КАК Наименование,
	|	АукЭлементыДанныхАукционов.ТехническоеНаименование КАК ТехническоеНаименование,
	|	АукЭлементыДанныхАукционов.ТипЗначения КАК ТипЗначения
	|ИЗ
	|	ПланВидовХарактеристик.АукЭлементыДанныхАукционов КАК АукЭлементыДанныхАукционов
	|ГДЕ
	|	АукЭлементыДанныхАукционов.%1 В(&Список)", пРеквизитДляОтбора);
	
	Запрос.УстановитьПараметр("Список", пСписок);
	
	сооРезультат = Новый Соответствие;
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл	
		
		сооРезультат.Вставить(Выборка.Наименование, Новый Структура("ТехническоеНаименование, ТипЗначения", Выборка.ТехническоеНаименование, Выборка.ТипЗначения));
		
	КонецЦикла;
	
	Возврат сооРезультат;
	
КонецФункции	

#КонецОбласти

#Область ЗначенияКонстант

Функция Константа_CountOfDaysInCurrentYear()
	Возврат 365;	
КонецФункции

#КонецОбласти


